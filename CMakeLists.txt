CMAKE_MINIMUM_REQUIRED ( VERSION 3.10 )

PROJECT ( CaNS Fortran)

FIND_PACKAGE ( MPI REQUIRED )

SET ( CMAKE_Fortran_COMPILER ${MPI_Fortran_COMPILER} )

ADD_DEFINITIONS ( -cpp -O3 -DDEBUG -DTIMING -DDOUBLE_PREC )

ADD_SUBDIRECTORY ( src )
